var nexusCtrl = angular.module('nexusCtrl', []);

nexusCtrl.controller('homeCtrl', ['$location', '$scope',
	function($location, $scope) {
		
}]);

nexusCtrl.controller('addCtrl', ['$location', '$scope',
	function($location, $scope) {
//		$location.path(add);
}]);

nexusCtrl.controller('searchCtrl', ['$location', '$scope',
	function($location, $scope) {
//		$location.path(add);
}]);